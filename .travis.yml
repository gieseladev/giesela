language: python
python:
  - "3.7-dev"
  - "3.6-dev"
install:
  - pip install pytest
  - pip install pytest-asyncio
  - pip install -r requirements.txt
script:
  - python -m pytest
notifications:
  email:
  on_failure: always
  on_success: always